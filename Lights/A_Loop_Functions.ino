void loop()
{
  computerLights();
  //I have been working on lights for my computer that I can control
  //via serial. That's what this function does. If you just want fire, blueFire, etc,
  //just cut out this function and put whichever one you want.
  
}
/* Available functions:
 * fire()
 * blueFire()
 * pink()
 * oneRedOneBlue()
 * barf()
 * blueFireRedFire()
 * idle(r, g, b, wait) 100 is a good value for wait
 * staticColor(r, g, b)
 */
